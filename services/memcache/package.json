{
  "name": "recipiece-memcache",
  "version": "1.0.0",
  "description": "memcache server for recipiece",
  "main": "dist/app.js",
  "scripts": {
    "test": "ts-mocha --paths -p tsconfig.test.json test/**/*.spec.ts",
    "build": "yarn link recipiece-common && tsc --build",
    "build-common": "cd ../common && yarn build && cd - && yarn build"
  },
  "author": "steven rosendahl",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.1",
    "cors": "^2.8.5",
    "cryo": "^0.0.6",
    "express": "^4.17.2",
    "redis": "^4.0.2"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/redis": "^4.0.11"
  }
}
