{
  "name": "recipes",
  "version": "1.0.0",
  "description": "recipe functionality for recipiece",
  "main": "dist/app.js",
  "scripts": {
    "test": "ts-mocha --paths -p tsconfig.test.json test/**/*.spec.ts",
    "build": "tsc --build",
    "run-dev": "node ./nodemon-runner.js"
  },
  "author": "Steven Rosendahl",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.2",
    "reflect-metadata": "^0.1.13",
    "tsyringe": "^4.6.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.15"
  }
}
